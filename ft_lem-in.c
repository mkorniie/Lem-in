#include "lemin.h"

int	main(void)
{
	t_room* structure;

	structure = ft_parse();
	
	// int len;
	// char* buf;


	// buf = (char*)malloc(sizeof(char) * (BUFSIZE + 1));
	// buf[BUFSIZE] = '\0';
	// len = 1;
	// while(len > 0)
	// {
	// 	len = read(0, buf, BUFSIZE);
	// 	buf[len] = '\0';
	// 	if (ft_strstr(buf, "##end"))
	// 		break;
	// 	// printf("%s", buf);
	// }
	return (0);
}